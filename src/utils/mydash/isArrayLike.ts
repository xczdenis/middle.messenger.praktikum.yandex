// import isNil from './isNil'
// import isLength from './isLength'
//
// function isArrayLike<T>(value: T): boolean {
//   return !isNil(value) && typeof value !== 'function' && isLength(value.length)
// }
//
// export default isArrayLike
